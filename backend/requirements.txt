fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
pytest>=8.0
# PostgreSQL (canonical store) — psycopg3 driver + pgvector + migrations
psycopg[binary]>=3.1
pgvector>=0.3
alembic>=1.13
redis>=5          # semantic cache + login limiter (compose service)
